<Window x:Class="RunScope.Views.MCUSimulatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:selectors="clr-namespace:DeviceSimulators.Selectors;assembly=DeviceSimulators"
        xmlns:local="clr-namespace:RunScope.Views"
        mc:Ignorable="d"
        Title="MCU Simulator" Height="450" Width="1000">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/DeviceSimulators;component/Resources/SimulatorsTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <ContentControl Content="{Binding}"
                        ContentTemplate="{StaticResource MCUSimulatorTemplate}"/>
                        
    </Grid>
</Window>
